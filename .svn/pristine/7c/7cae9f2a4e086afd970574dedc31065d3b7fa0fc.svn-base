img.vertical {
    width: 160px;
    display: block;
    margin: 0 auto;
}


ul.ul-table{
    list-style: none;
    width: 100%;
    display: table;
    margin: 0;
    padding: 25px;
    color: var(--mv-text-color);
    font-size: 12px;

    .row{
        display: flex;
        flex-direction: row;
        width: 100%;
        flex-wrap: wrap;
        text-align: right;
        padding: 6px 0;
        margin-right: auto;

        &:not(.header){
            border-bottom: 1px solid var(--mv-text-opaque-color-2);
        }

        &.header{
            color: var(--mv-primary-color);
            font-size: 14px;
            border-bottom: 1px solid var(--mv-text-opaque-color-2);
        }      
    }
}