import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TopicsHelpCenterComponent } from './topics-help-center.component';

describe('TopicsHelpCenterComponent', () => {
  let component: TopicsHelpCenterComponent;
  let fixture: ComponentFixture<TopicsHelpCenterComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TopicsHelpCenterComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TopicsHelpCenterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
