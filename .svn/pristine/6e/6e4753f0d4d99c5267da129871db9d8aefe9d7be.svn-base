import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PayuColletComponent } from './payu-collet.component';

describe('PayuColletComponent', () => {
  let component: PayuColletComponent;
  let fixture: ComponentFixture<PayuColletComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PayuColletComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PayuColletComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
